spring:
  config:
    activate:
      on-profile: test

product-service:
  url: http://localhost:8080

---

spring:
  config:
    activate:
      on-profile: local

product-service:
  url: http://localhost:8080

---

spring:
  config:
    activate:
      on-profile: dev

product-service:
  url: http://localhost:8080